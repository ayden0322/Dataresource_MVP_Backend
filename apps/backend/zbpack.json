{
  "build_command": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter backend build",
  "start_command": "cd apps/backend && node dist/main.js",
  "install_command": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": "nodejs",
  "node_version": "18",
  "buildpack": true,
  "env": {
    "NODE_ENV": "production"
  }
}

